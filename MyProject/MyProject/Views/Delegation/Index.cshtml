@model IEnumerable<MyProject.Models.Delegation>

@{
    ViewBag.Title = "Index";
}

<h2>Delegations</h2>

<p>
    @Html.ActionLink("Add New Delegation Type", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DelegationType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DelegationType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DelegationId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DelegationId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DelegationId })
        </td>
    </tr>
}

</table>

@Html.ActionLink("Back", "../Home/Admin")